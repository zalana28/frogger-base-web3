// Base Mainnet only (chainId 8453). This game is mainnet-only.
// Re-export wagmi's maintained chain definition so we get official
// multi-RPC fallbacks and block explorer metadata for free.
export { base } from 'wagmi/chains';
